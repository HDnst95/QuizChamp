<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quizchamp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.QuizChamp">
        <activity
            android:name=".activity.LoginRegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.SinglePlayerActivity"
            android:exported="true" />

        <activity
            android:name=".activity.HighscoreActivity"
            android:exported="true" />

        <activity
            android:name=".activity.MultiPlayerActivity"
            android:exported="true" />

        <activity
            android:name=".activity.ViewHighscoresActivity"
            android:exported="true" />

        <activity
            android:name=".activity.MainMenuActivity"
            android:exported="true" />

        <activity
            android:name=".activity.MatchmakingActivity"
            android:exported="true" />

        <!-- Inside <application> tag -->
        <meta-data
            android:name="com.google.android.gms.google-services"
            android:value="@string/server_client_id" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>
